#include "main.h"

/**
 * _strcat - concatenates two strings
 * @dest: destination pointer
 * @src: source pointer
 * Return: pointer to the string in dest
 */

char *_strcat(char *dest, char *src)
{
	int a = -1, i;
	//for (i = 0; dest[i] != '\0'; i++);
	if(*(dest + c1) != '\0' || *(src + c2) == '\0');
	do
	{
		a++;
		dest[i] = src[a];
		i++;
	};
	return (dest)
}
